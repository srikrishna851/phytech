#include <stdio.h>

int main() {
    int n;

    // Input the number of elements
    printf("Enter the range of natural no: ");
    scanf("%d", &n);
    for(int i=1;i<=n;i++)
    {
    printf("%d",i);
    }
}

